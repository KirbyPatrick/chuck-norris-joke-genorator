import React from "react";
import Button from "@material-ui/core/Button";

const NewJokeButton = () => {
  return (
    <div className="buttonContainer">
      <Button variant="contained">New Fact</Button>
    </div>
  );
};

export default NewJokeButton;
